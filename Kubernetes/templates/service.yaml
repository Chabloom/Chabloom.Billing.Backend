apiVersion: v1
kind: Service
metadata:
  name: {{ include "Chabloom.Billing.Backend.fullname" . }}
  labels:
    {{- include "Chabloom.Billing.Backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Chabloom.Billing.Backend.selectorLabels" . | nindent 4 }}
